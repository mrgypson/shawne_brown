/** Public id — safe to commit; override via SANITY_PROJECT_ID if needed. */
export const SANITY_PROJECT_ID_DEFAULT = 'yrca4rxr';

export const SANITY_DATASET_DEFAULT = 'production';

export const SANITY_API_VERSION_DEFAULT = '2024-11-21';

/** When Sanity has no asset yet. */
export const PLACEHOLDER_IMAGE_SRC = '/images/projects/placeholder.svg';
