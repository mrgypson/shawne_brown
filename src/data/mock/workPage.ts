import type { WorkPageContent } from '../../types/workPage';

export const workPageContent: WorkPageContent = {
	title: 'Work',
	showTitle: true,
	subheading: 'Series and projects, listed in current order of emphasis.',
	showSubheading: true,
};
