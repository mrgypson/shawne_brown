# Optional — hosted Studio already uses defaults from sanity.config.ts (shawne-brown.vercel.app).
# Copy to `.env.production` only if you need to override preview URL or allowOrigins for another host.

# SANITY_STUDIO_PREVIEW_URL=https://other-host.example
# SANITY_STUDIO_ALLOW_ORIGINS=https://other-host.example,https://*.vercel.app
